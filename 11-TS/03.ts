// A function that takes two integers and returns an integer
function add(x: number, y: number): number {
    return x + y
}

console.log(add(2, 3))

// A function that does not return anything (void function)
function log(msg: string): void {
    console.log(msg)
}

log('Hello world')
